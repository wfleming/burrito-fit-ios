# this is so you don't need to have a global gem install
# (which I always avoid because global gems with bins always
# always end up with a bunch of duplicates
source 'https://rubygems.org'

gem 'cocoapods'
